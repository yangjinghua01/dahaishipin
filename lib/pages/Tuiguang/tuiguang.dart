import 'package:flutter/cupertino.dart';

class Tuiguang extends StatelessWidget {
  const Tuiguang({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
