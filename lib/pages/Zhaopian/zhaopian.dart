import 'package:flutter/cupertino.dart';

class Zhaopian extends StatelessWidget {
  const Zhaopian({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("zhaopian"),);
  }
}
